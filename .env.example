# Copier en .env et remplir les valeurs réelles avant le déploiement
SECRET_KEY=django-insecure-change-me-in-production
DEBUG=False
ALLOWED_HOSTS=votre-domaine.onrender.com,localhost
DATABASE_URL=postgres://cityflow:motdepasse@db:5432/cityflow_db
DB_PASSWORD=cityflow123

# CORS — remplacer par l'URL réelle de l'app Flutter Web
CORS_ALLOWED_ORIGINS=https://votre-app-flutter.web.app

# Sécurité cookies (True en production avec HTTPS)
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# Moteur de routing (écran Trajet) — voir scripts/prepare_osrm_data.sh
OSRM_URL=http://osrm:5000
